# Snapshot — 2026-05-22 17:00 IST (compact system online)

**Session window:** May 22 ~15:00 → 17:00 IST (compact-system build segment, post-V3-lock)
**Active phase:** **Pre-Phase 0** — V3 architecture locked; compact system online; Phase 0 demo polish queued (NOT started)
**Active branch (onsite-hub):** `feat/pwa-whatsapp-wave`
**Last commit (onsite-hub):** `445aa76` `chore: deploy script for Vercel preview + prod`
**Context budget at compact:** ~92% (very heavy session — V3 + compact system all in one)

## Goal of this session segment

After locking V3 architecture (snapshot 1500), build a durable compact handoff system so no session loses context across `/compact` boundaries. Produce: (a) reusable PRE-COMPACT-PROMPT.md + POST-COMPACT-PROMPT.md templates Dhruv can copy-paste, (b) `compact/` folder for cumulative `SNAPSHOT-*.md` archive, (c) README explaining the flow, (d) THIS snapshot capturing the system itself.

## Shipped (durable)

- `Onsite/task-ai/compact/` — new folder (cumulative archive, never delete)
- `Onsite/task-ai/compact/README.md` — system overview + flow + rules
- `Onsite/task-ai/compact/PRE-COMPACT-PROMPT.md` — verbose 6-step template (paste before `/compact`)
- `Onsite/task-ai/compact/POST-COMPACT-PROMPT.md` — verbose 6-step template (paste after `/compact`)
- `Onsite/task-ai/compact/SNAPSHOT-2026-05-22-1500.md` — V3-lock snapshot (the earlier session boundary)
- `Onsite/task-ai/compact/SNAPSHOT-2026-05-22-1700.md` — THIS file
- `Onsite/task-ai/STATE.md` — updated with full "V3 ARCHITECTURE LOCK + COMPACT SYSTEM" entry at top of Recent Changes
- `Onsite/task-ai/CLAUDE.md` — updated to list compact/ folder in the V3 doc map (8th entry in read order)

Also durable from this overall session (already captured in SNAPSHOT-1500 but worth re-listing for completeness):
- `BRD-V3.md`, `PRD-V3.md`, `ARCHITECTURE-V3.md`, `HLD-V3.md`, `LLD-V3.md`
- `CLAUDE.md` rewritten to point at V3
- `MEMORY.md` rewritten with 35 entries (M-01 through M-35)

## In progress (uncommitted, recoverable on resume)

**Everything in `Onsite/task-ai/` from this session is uncommitted on `Onsite` repo.** Specifically:
- All 7 V3 docs (BRD/PRD/ARCH/HLD/LLD/CLAUDE/MEMORY)
- All 4 new compact files (README/PRE/POST/SNAPSHOT-1500)
- This snapshot (SNAPSHOT-1700)
- Plus pre-existing untracked files: `ANGELINA-ADDON-DISPATCH.md`, `ARCHITECTURE-V2.md`, `DECISIONS-ADDON.md`, `HLD-ADDON.md`, `PRD-ADDON.md`, `ROADMAP-ADDON.md`, `WHATSAPP-SETUP.md`, `PWA-NOTES.md`
- Plus `database/009_push_subscriptions.sql` + `database/013_followup_fields.sql` (untracked)

**Recommended commit before /compact:**
```bash
cd /Users/apple/Aiwithdhruv/AI\ Development/Claude/Onsite
git add task-ai/ database/
git commit -m "docs(task-ai): V3 architecture lock + compact handoff system"
git push
```

No `onsite-hub` (code) changes this segment — last commit `445aa76` still HEAD on `feat/pwa-whatsapp-wave`.

## Decisions made this session (segment)

- **Compact system filename convention:** `SNAPSHOT-YYYY-MM-DD-HHMM.md` for natural alphabetical sort (latest = lexicographically last).
- **Compact folder is cumulative archive:** never delete old snapshots — historical evolution reference.
- **Pre-compact prompt is verbose:** lists every V3 doc by path, every reference repo by path, every section the snapshot must contain. Idempotent — safe to re-run.
- **Post-compact prompt is verbose:** 12 hard rules from MEMORY.md inlined (so they survive even if Claude doesn't read MEMORY.md fully). Phase-specific deep-dive doc map per phase. Mandatory wait at end (don't start work until Dhruv says go).
- **CLAUDE.md gets 8th read item:** the `compact/` folder is now part of the canonical doc map.
- **MEMORY.md untouched this segment:** no new non-obvious gotcha discovered (writing docs doesn't surface bugs).

## Blockers / pending Dhruv input

1. **Commit + push V3 docs + compact folder** to GitHub before `/compact` (recommended) — see commit cmd above. Once pushed, durable backup.
2. **Vercel CLI auth** still pending — `vercel login` or `VERCEL_TOKEN` paste.
3. **PWA icons** — generate via pwabuilder.com/imageGenerator with sparkle logo + bg `#0f0a26` → unzip into `onsite-hub/public/icons/task-bot/`.
4. **Meta App Secret** — get from Meta dashboard → App Settings → Basic. Currently dev bypass active.
5. **OpenWeatherMap API key** — for Phase 10 weather alerts.
6. **Razorpay creds** — Phase 11.

## New learnings added to MEMORY.md

None this segment. (Writing the compact system itself didn't surface non-obvious gotchas — it's process, not bugfix. M-01 through M-35 are unchanged.)

## Next concrete action (when we resume)

1. **Commit + push the uncommitted V3 + compact work** to `Onsite` repo on GitHub:
   ```bash
   cd /Users/apple/Aiwithdhruv/AI\ Development/Claude/Onsite
   git add task-ai/ database/
   git commit -m "docs(task-ai): V3 architecture lock + compact handoff system"
   git push
   ```

2. **Start Phase 0 — Demo polish** (1-2 days):
   - Restart dev server: `cd onsite-hub && npm run dev -- --port 3001`
   - Smoke test all 14 Onsite tools end-to-end (use real JWT)
   - Install Sentry + Langfuse + Posthog (30-min observability baseline)
   - Generate demo tenant seed script `onsite-hub/scripts/seed-demo.ts` (5 projects, 80 tasks, 200 progress, 30 BOQs, 20 invoices, 50 photos)
   - Restart Cloudflare tunnel for demo URL: `cloudflared tunnel --url http://localhost:3001 > /tmp/cloudflared.log 2>&1 &`
   - Prep 5-min demo script for Akshansh/Sumit

3. **Then Phase 1 — Memory port** (3-4 days):
   - Apply migration `011_memory_entries.sql` via `supabase db push` (use SUPABASE_ACCESS_TOKEN_TASK_AI + password `Dhruvtomar7008@`)
   - Port `onsite-hub/src/lib/memory/repository.ts` from `angelina-vercel-clean/src/lib/memory.ts`
   - Add `save_memory` + `recall_memory` tools in `onsite-hub/src/app/api/task-bot/memory/{save,recall}/route.ts`
   - Wire into LangGraph agent tool catalogue
   - Test: "Remember my project Arohan" → next session → "What was my project?" → bot recalls

4. **Then Phase 2 — 2-tier LLM router + cost guardrails + Upstash Redis cache** (3 days)

## Read order for next Claude (always)

1. `Onsite/task-ai/CLAUDE.md` (auto-loaded — V3 doc map, 8th entry = compact/)
2. `Onsite/task-ai/MEMORY.md` (35 entries — every M-NN, read before any code)
3. `Onsite/task-ai/STATE.md` (top = most recent Recent Changes entry from today)
4. **THIS snapshot** (`compact/SNAPSHOT-2026-05-22-1700.md`)
5. `Onsite/task-ai/ARCHITECTURE-V3.md` (§0 decisions + §17 tool catalogue minimum)
6. For Phase 0 prep: `Onsite/task-ai/ARCHITECTURE-V3.md` §16 (demo tenant) + `Onsite/onsite-hub/CLAUDE.md` (existing system)
7. For Phase 1 (next major work): `Onsite/task-ai/LLD-V3.md` §2 (migration 011) + §3 (memory API) + §4 (save/recall tool specs) + `angelina-vercel-clean/src/lib/memory.ts` + `angelina-vercel-clean/sql/001_memory_schema.sql`

## Files actively being edited

None — everything clean. All 3 file modifications (STATE.md, CLAUDE.md, this snapshot) are complete writes, not half-edits.

## Tunnels / running processes

- Cloudflare tunnel: **dead** (was `complicated-unsubscribe-ticket-inspection.trycloudflare.com`, killed by inactivity hours ago). To restart: `cloudflared tunnel --url http://localhost:3001 > /tmp/cloudflared.log 2>&1 &`
- Dev server (Next.js port 3001): **stopped**. To restart: `cd onsite-hub && npm run dev -- --port 3001`
- Modal apps: none deployed yet (planned Phase 5)
- Background tasks: none

## Supabase state

- Project ref: `xcvrdjhnvngfzczumquq` (Onsite_AI)
- Migrations applied: **001-010** (all confirmed on remote)
- Migrations pending (defined in LLD-V3.md §2 but NOT yet written as SQL files): **011-018**
  - 011_memory_entries (Phase 1)
  - 012_documents (Phase 5.5)
  - 013_rag_chunks (Phase 5)
  - 014_alerts (Phase 10)
  - 015_webhooks (Phase 10)
  - 016_progress_geo (Phase 10)
  - 017_vendor_scores (Phase 10)
  - 018_consent_audit (Phase 11)
- DB password: `Dhruvtomar7008@` (in `~/.aiwithdhruv-secrets`)
- PAT env name: `SUPABASE_ACCESS_TOKEN_TASK_AI` (in `~/.aiwithdhruv-secrets`)

## Tooling state

- **Vercel CLI authed:** NO (`vercel whoami` returns "No existing credentials"). Blocker for any deploy.
- **Vercel project linked:** NO (no `.vercel/` folder in `onsite-hub/`).
- **Vercel envs synced:** NONE (script exists at `onsite-hub/scripts/deploy-to-vercel.sh` ready to push 22 vars when authed).
- **DNS subdomain:** None set up. Plan: `taskai.aiwithdhruv.com` once Vercel authed + deploy succeeds.
- **GitHub gh CLI:** Authed for `aiagentwithdhruv` account (push works).

## Cost model snapshot (still locked per V3)

| At 1 lakh users × 5 q/day | ₹/month |
|---|---|
| Pure Claude (baseline) | ₹37.5L |
| 2-tier router (decision #2) | ₹17L |
| + semantic cache (Phase 2) | ₹12L total (LLM + infra) |
| **Per user** | **₹12** |

Gross margin at ₹100/user/month entry tier = 88%.

## Final reminder to next Claude

You're resuming with a heavy, fully-documented architecture. The temptation will be to start coding immediately — DON'T. Read the V3 doc set + MEMORY.md first. Many seemingly-good ideas have already been considered and have a decision logged. Don't re-litigate locked decisions (the 6 sign-offs in ARCHITECTURE-V3 §0) without explicit Dhruv input.

Phase 0 demo polish is the next concrete step. Keep it scoped — Sentry + Langfuse + demo tenant + tunnel. No new features. Then commit, demo, get Akshansh/Sumit feedback, then Phase 1 memory port.
